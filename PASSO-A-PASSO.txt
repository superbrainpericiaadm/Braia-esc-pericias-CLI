========================================
COMO CRIAR UM AGENTE CLAUDE CODE + TELEGRAM v3
(Bot externo Python + agent-manager + subagentes)
========================================

Repositorio:
https://github.com/{{GITHUB_USERNAME}}/agente-claude-telegram-setup-alunos-denderson

VERSAO 3 (abril 2026) - novidades vs v2:
- agent-manager.py via PM2 (porta 3600 + Caddy + Cloudflare tunnel)
- 5 subagentes especializados (paulo-dev, juliana-ops, jonathan-copy, rafael-projetos, davi-sdr)
- Audio bidirecional (Whisper + ElevenLabs)
- Memoria vetorial PostgreSQL + pgvector

O que voce tem no final:
- Agente principal (ex: Jonas-bot) que conversa via Telegram
- 5 subagentes (paulo-dev, juliana-ops, jonathan-copy, rafael-projetos, davi-sdr)
- Memoria PostgreSQL + pgvector
- Bot Python sempre vivo (independente do Claude)
- Audio bidirecional (Whisper + ElevenLabs)
- agent-manager HTTP em https://AGENTE.dominio.com
- Auto-restart, healthcheck, backup conversas, relatorio diario


========================================
PRE-REQUISITOS
========================================

- VPS Ubuntu 22.04+ ou macOS 13+
- 4 GB RAM minimo, 8 GB recomendado
- Conta Claude Pro ou Max (do aluno)
- Conta Telegram (do aluno)
- (Opcional) OPENAI_API_KEY (audio entrada)
- (Opcional) ELEVENLABS_API_KEY (audio saida)
- (Opcional) GitHub PAT + Vercel + Cloudflare DNS (deploy publico)


========================================
PASSO 1 - CRIAR BOT TELEGRAM
========================================

1. Telegram, busca @BotFather
2. Manda: /newbot
3. Nome: ex "Assistente do Jonas"
4. Username (termina em bot): ex jonas_assistente_bot
5. Copia o TOKEN retornado

Pegar ID Telegram do dono:
1. Busca @userinfobot, manda qualquer msg
2. Copia o ID numerico (ex 1672601444)


========================================
PASSO 2 - INSTALAR DEPENDENCIAS (1 COMANDO)
========================================

Linux (root):
   curl -fsSL https://raw.githubusercontent.com/denderson2013-bot/agente-claude-telegram-setup-alunos-denderson/main/bootstrap.sh | bash

macOS (usuario normal):
   curl -fsSL https://raw.githubusercontent.com/denderson2013-bot/agente-claude-telegram-setup-alunos-denderson/main/bootstrap.sh | bash

Instala: Node 22, Python 3, ffmpeg, Claude Code CLI 2.1.118 (pinado),
tmux, PostgreSQL 16 + pgvector, Caddy, pm2 globalmente.

Tempo: 3-5 min.


========================================
PASSO 3 - LOGAR NA CONTA CLAUDE
========================================

   claude auth login --claudeai

Aluno copia o link, abre no navegador, loga (Pro ou Max),
autoriza, copia o codigo, cola no terminal.


========================================
PASSO 4 - INICIAR O CLAUDE
========================================

Linux:
   cd /root && claude --dangerously-skip-permissions

Mac:
   cd ~ && claude --dangerously-skip-permissions


========================================
PASSO 5 - DEIXAR O CLAUDE FAZER O RESTO
========================================

Dentro do Claude, cola:

   Leia o arquivo SETUP-AGENTE.md e execute todos os passos.
   Me faca perguntas quando precisar de informacao minha.


========================================
PASSO 6 - RESPONDER AS PERGUNTAS
========================================

Obrigatorias:
1. Nome do agente (minusculas): ex jonas
2. Nome do dono: ex Jonas
3. TELEGRAM_BOT_TOKEN (do passo 1)
4. ID numerico Telegram (do passo 1)
5. Personalidade/negocio do dono

Opcionais (pode adicionar depois):
6. OPENAI_API_KEY
7. ELEVENLABS_API_KEY + VOICE_ID
8. GITHUB_TOKEN + VERCEL_TOKEN + CLOUDFLARE_API_TOKEN (deploy)
9. Config Clone SDR (oferta, pitch, preco, link)

O Claude faz tudo sozinho:
- Cria usuario AGENTE
- Cria DB PostgreSQL com schema (vetorial)
- Cria /opt/AGENTE/ + /opt/AGENTE-bot/
- Cria CLAUDE.md personalizado (3 fases + orquestradora)
- Cria 5 subagentes
- Cria bot.py (audio bidirecional)
- Cria agent-manager.py via PM2 (porta 3600)
- Configura Caddy + Cloudflare tunnel
- 2 systemd services + crons de healthcheck/backup
- Inicia tudo, valida, entrega

Tempo: 5-15 min.


========================================
PASSO 7 - TESTAR
========================================

1. Aluno abre o Telegram, busca @bot_username
2. Manda "oi"
3. Ve reacao olhinho (bot recebeu)
4. Ve "digitando..."
5. Recebe resposta da Naia em FASE 1 (em ate 10s)
6. Naia executa em FASE 2
7. Naia entrega em FASE 3

Audio:
1. Manda audio (voice message)
2. Bot baixa, Whisper transcreve PT-BR
3. Naia processa
4. Naia decide responder em texto OU audio


========================================
ARQUITETURA v3 (RESUMIDA)
========================================

Telegram <-> [Bot Python systemd 24/7] <-> tmux + outbox JSON
                                                |
                                                v
                                       [Claude Code (tmux)]
                                                |
                                                v
                                  [agent-manager.py PM2 :3600]
                                                |
                  +----+----+----+-------+-----+----+
                  v    v    v    v       v          v
              paulo juliana jon rafael davi-SDR    ...

              [Caddy reverse proxy] -> https://AGENTE.dominio.com


PROTOCOLO DE CONVERSA (REGRA SUPREMA):
- FASE 1 (10s): "Entendi. Vou X (delegar pra Y) porque Z. Tempo: N"
- FASE 2: executa em background (typing automatico)
- FASE 3: "Pronto. <links, status, tempo>"


========================================
COMANDOS DO DIA A DIA
========================================

Status geral:
   systemctl status AGENTE
   systemctl status AGENTE-bot
   pm2 status

Logs ao vivo:
   tail -f /opt/AGENTE/logs/agent.log
   tail -f /opt/AGENTE-bot/logs/bot.log
   pm2 logs agent-manager

Tela do Claude:
   su - AGENTE -c "tmux attach -t AGENTE"
   (sair sem fechar: Ctrl+B depois D)

Restart:
   systemctl restart AGENTE
   systemctl restart AGENTE-bot
   pm2 restart agent-manager

Editar personalidade:
   nano /opt/AGENTE/CLAUDE.md && systemctl restart AGENTE

Editar subagente:
   nano /opt/AGENTE/.claude/agents/paulo-dev.md
   (sem restart)

Ver memoria:
   sudo -u AGENTE psql -d AGENTE_memory \
     -c "SELECT COUNT(*) FROM conversation_history"


========================================
CRONS ATIVOS
========================================

*/2 * * * *   /opt/AGENTE-bot/healthcheck.sh
0 */2 * * *   consolidate-conversations.py
30 * * * *    heartbeat.sh
0 9 * * *     daily-memory-report.py


========================================
CUSTOS MENSAIS
========================================

- VPS 4-8 GB: R$40-120
- Claude Max: US$100 (Pro tambem serve)
- OpenAI Whisper: ~US$0.006/min audio
- ElevenLabs: free 10k chars OU $5/mes basic
- Telegram + PostgreSQL: gratis

Total: R$600-700/mes


========================================
PROBLEMAS COMUNS
========================================

"Mensagens duplicadas no Telegram"
   Plugin oficial nao deve estar ativo. Confere:
   cat /home/AGENTE/.claude/settings.json | grep enabledPlugins
   Se tiver telegram, REMOVE e reinicia.

"https://AGENTE.dominio.com da 502"
   Manual: pm2 restart agent-manager && systemctl reload caddy

"Audio nao transcreve / nao sai"
   Confere chaves no .env do bot:
   cat /opt/AGENTE-bot/.env | grep -E 'OPENAI|ELEVEN'

"Agente esquece tudo"
   Confere consolidate-conversations:
   crontab -u AGENTE -l | grep consolidate

"VPS reboot e nao voltou"
   systemctl is-enabled AGENTE AGENTE-bot
   pm2 startup (re-gera comando, executa)


========================================
FIM v3
========================================

Issues: https://github.com/{{GITHUB_USERNAME}}/agente-claude-telegram-setup-alunos-denderson/issues
